services:
  db:
    image: postgres
    restart: always
    shm_size: 128mb
    volumes:
      "./postgres-data/data:/var/lib/postgres/data/"
    # env_file:
    #   - .env
    environment:
      POSTGRES_USER: username
      POSTGRES_PASSWORD: password
      POSTGRES_DB: default_database